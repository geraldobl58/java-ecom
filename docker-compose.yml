name: ecom

services:
  ecom-db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=ecomdb
    volumes:
      - /Volumes/Backup/DockerSSD/ecom/postgres:/var/lib/postgresql/data